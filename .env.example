# AG rider — App environment
# Copy to .env and configure the backend and OTP widget.
#
#   cp .env.example .env

# ─── Backend API ──────────────────────────────────────────────────────────────
# Before deploy: http://localhost:3000  OR  http://<your-lan-ip>:3000 for phone testing
# After deploy:  paste API Gateway URL from `cd backend && npm run deploy`
# After deploy: Railway production API
EXPO_PUBLIC_API_URL=https://myproject-production-e2d4.up.railway.app

# ─── MSG91 OTP Widget (client-side SDK) ────────────────────────────────────────
# From MSG91 → OTP → your widget (enable Mobile Integration)
EXPO_PUBLIC_MSG91_WIDGET_ID=
EXPO_PUBLIC_MSG91_AUTH_TOKEN=
